syntax = "proto3";

/* Copyright 2022 Danny McClanahan */
/* SPDX-License-Identifier: AGPL-3.0-only */

package emdocs.proto.buffers;

/* A tag referring to a specific buffer. */
message BufferId {
  /* A unique id, typically a UUID v4. */
  optional bytes uuid = 1;
}
